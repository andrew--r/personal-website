{
  "private": true,
  "name": "andreyromanov.com",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "clean": "rm -rf build",
    "prestart": "pnpm clean && gulp",
    "start": "gulp watch & eleventy --serve",
    "prebuild": "pnpm clean",
    "build": "gulp && eleventy"
  },
  "devDependencies": {
    "ar-11ty-transforms": "*",
    "ar-shared-styles": "*"
  }
}
