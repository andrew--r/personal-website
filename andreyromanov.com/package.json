{
  "private": true,
  "name": "andreyromanov.com",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "clean": "rm -rf build",
    "prestart": "yarn clean && gulp",
    "start": "gulp watch & eleventy --serve",
    "prebuild": "yarn clean",
    "build": "gulp && eleventy"
  },
  "devDependencies": {}
}
